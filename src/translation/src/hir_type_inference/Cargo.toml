[package]
name = "nitrate_hir_type_inference"
version = "0.1.0"
edition = "2024"

[dependencies]
nitrate_hir = { path = "../hir" }
nitrate_diagnosis = { path = "../../../diagnosis" }
interned-string = { version = "0.3.0", features = ["serde"] }
