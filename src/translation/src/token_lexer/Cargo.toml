[package]
name = "nitrate_token_lexer"
version = "0.1.0"
edition = "2024"

[dev-dependencies]
criterion = { version = "0.7.0", features = ["html_reports"] }

[[bench]]
name = "lex_bench"
harness = false

[dependencies]
nitrate_diagnosis = { path = "../../../diagnosis" }
nitrate_token = { path = "../token" }
log = "0.4.27"
ordered-float = { version = "5.0.0", features = ["serde"] }
