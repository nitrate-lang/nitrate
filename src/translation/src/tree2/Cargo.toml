[package]
name = "nitrate_tree2"
version = "0.1.0"
edition = "2024"

[dependencies]
nitrate_nstring = { path = "../nstring" }
nitrate_token = { path = "../token" }

ordered-float = "5.0.0"
serde = { version = "1.0.219", features = ["derive", "rc"] }
