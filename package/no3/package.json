{
  "name": "no3",
  "version": "1.0.0",
  "description": "Nitrate Language Package Manager and Build Tool",
  "bin": "index.js",
  "scripts": {
    "postinstall": "npm remove -g no3 && ((curl --proto '=https' --tlsv1.2 -sSfL https://nitrate.dev/install.sh | sh) || echo \"Failed to install the no3\")"
  },
  "author": "Wesley Jones",
  "license": "LGPL-2.1+"
}